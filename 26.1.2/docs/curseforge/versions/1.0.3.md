<h2>v1.0.3 - Sweep Effect Fix &amp; Translations</h2>

<h3>Fix</h3>
<ul>
<li><strong>Sweep arc position</strong>: particle now spawns at eye level, 1.5 blocks forward — visible in first person</li>
<li><strong>Missing translation key</strong>: added <code>isShowingSweepEffect</code> entry to <code>en_us.json</code></li>
</ul>

<h3>Technical Changes</h3>
<ul>
<li><code>WeaponEffectManager.spawnSweepParticle()</code> — uses <code>player.getEyePosition().add(look.scale(1.5))</code></li>
<li>Replaced stale <code>isShowingWeaponTrails</code> translation with <code>isShowingSweepEffect</code></li>
</ul>

<hr>

<p><strong>JAR</strong>: <code>dinamyc_combat-26.1.2-neoforge-1.0.3.jar</code></p>