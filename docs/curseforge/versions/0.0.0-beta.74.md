### Fully delegated to PAC beta.20 — no custom first-person mixins

PAC beta.20 now includes hand position computation from arm rotation (`sin(yaw)*armLength`, `sin(pitch)*armLength*pitchFactor`) directly in `HANDS_ONLY_ARM` mode.

**Changes:**

- Upgraded to PAC beta.20 (built-in hand position from arm rotation)
- Removed all custom first-person mixins — PAC handles everything
- Configured `HANDS_ONLY_ARM` with `setArmRotationScale(1.0f)`, `armLength=8.0f`, `pitchFactor=0.6f`
- First-person weapon now swings through a full arc (position + rotation) matching third person
- Zero custom mixins — Dinamyc Combat only configures PAC parameters
